
import Typography from '@mui/material/Typography';

const Dashboard = () => {
    return (
        <div>
            <Typography variant="h4" gutterBottom>
                Dashboard
            </Typography>
            <Typography paragraph>
                Welcome to CampusMind AI. Select a tool from the sidebar to get started.
            </Typography>
        </div>
    );
};

export default Dashboard;
